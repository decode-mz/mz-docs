import { isValid, format, generate } from "./bi";
import {} from "./nuit";

/**
 * All BI methods
 */
const bi = { isValid, format, generate };

/**
 * All NUIT methods
 */
const nuit = {};

export { bi as bi };
